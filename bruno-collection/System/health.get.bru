meta {
  name: Health Check
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/{{api_version}}/health
}

body:json {
  "description": "Health check endpoint with database and storage connectivity verification",
  "expected_response": {
    "status": "ok",
    "services": {
      "database": "connected",
      "storage": "connected",
      "api": "operational"
    }
  }
}