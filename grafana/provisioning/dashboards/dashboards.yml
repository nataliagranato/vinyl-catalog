apiVersion: 1

providers:
  - name: vinyl-catalog
    orgId: 1
    folder: "Vinyl Catalog"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
